# 유스튜디오 MCP 구축 — 프로젝트 문서 전체 백업

> 백업일 2026-08-27. claude.ai 프로젝트 **"유스튜디오 MCP 구축"** 삭제 전에 문서 6개를 한 파일로 묶은 것.
> 원본 프로젝트는 삭제 예정. 이 파일이 유일한 사본이 된다.
>
> **먼저 알아둘 것 — 아직 열려 있는 항목 2개**
> 1. **키 교체 대기** — GROQ · EVOLINK · SPEECHMATICS 3건이 노출됐고 아직 교체 안 됨.
>    서버가 운영 중이라 폐기하면 안 되고, 순서는 `새 키 발급 → 반영 → 확인 → 옛 키만 무효화`.
> 2. **주간 하네스 정비 리마인더(금요일 10시 카톡)** — 프로젝트를 접으면 삭제할 것.
>    계정 스케줄 목록에는 없으니 Cowork 데스크톱 앱에 로컬 저장돼 있을 수 있다. 앱에서 확인.
>
> 참고: `유스튜디오 상태 점검(백업+데이터수집)` 매일 알림은 **유지해야 한다.** 이건 MCP 구축이
> 아니라 유튜브 대시보드 운영 점검이고, 프로젝트를 지워도 사라지지 않는다.

---
---

# 1. 진행상황.md — 영화롱폼 최종 인계 기록

> 원문 갱신 2026-08-27. 가장 중요한 문서.

# 유스튜디오 MCP (영화롱폼) — 구축 완료·배포 운영 중

> 갱신: 2026-08-27. **서버 구축 → 클라우드 배포 완료, 운영 중. 작업 폴더 정리 완료.**
> 이 문서는 이 프로젝트의 최종 인계 기록이다. 새 작업은 별도 프로젝트에서 시작한다.

## 무엇이 완성됐나
영화를 주면 서버가 지휘해 롱폼 리캡을 만들고, **더블클릭하면 전부 제자리인 프리미어 파일(.prproj)** 이 나온다. 볼케이노와 같은 형태.
- 10단계: setup→start→probe→transcript→brief→select→script→voice→subtitle→export
- 산출물: `.prproj`(본선) + XML·SRT·나레이션 믹스·manifest(부산물, 규격 스위치로 XML 폴백)
- 태그 v0.1-select ~ v0.8-wordsync, 스모크 141항목, 게이트 14개
- **배포 완료**: Cloudflare Workers(`서버/wrangler.jsonc`·`배포.sh`·`.last_deploy`, 배포 커밋 3개). **인증 닫힘 실측 확인** — 토큰 없음/틀림 401, 맞으면 200, `/health` auth:true

## 핵심 자산 (재사용 가치 큼)
- **스타일/영화롱폼/**: 지침서(지무비체 나레이션·구간선택·자막·줄바꿈) + 정답지(지무비 10편을 우리 자로 실측) + 규격
- **서버/runner/**: 계측기 일습 — 조립_prproj.py(도너 복제·치환), 분석_계측/판독/집계, 줄바꿈검사, 나레커버율검사, 도너_대조/규격갱신
- **설계/**: prproj 구조 지식, 한국어 줄바꿈 규칙, 자막종류체계, 진단일지(§1~§25 사고 전수), 주간정비 기록부
- **분석/지무비/**: 판독·계측·요약 86파일 + 링크 10개(원본은 2026-08-27 삭제, URL로 재취득 가능)

## 배운 것 (다음 프로젝트에도 유효)
1. **추정하지 말고 잰다** — 전사 늘어진 발화, TTS 길이, 글자 타임스탬프, 대사 선행, 자막 잉크 높이… 추정이 실측에 진 사례가 6번
2. **번호가 아니라 내용으로 짝을 맞춘다** — 나레 캐시 오염(14블록), 대사 번역 순번 밀림(d057~) 둘 다 번호 신뢰가 원인
3. **게이트는 수치만이 아니라 내용도 본다** — ASR로 실제 발화를 듣고 대본과 대조하는 게이트가 사고를 잡았다
4. **기존 값을 덮기 전에 같은 축으로 다시 잰다** — 가족 실측값(자막 크기·자수)이 세 번 다 맞았는데 틀린 축으로 덮어써 되돌렸다
5. **싼 길을 먼저 찾는다** — 도너 prproj를 프리미어에서 직접 고치니 코드 변경 0으로 자막 크기·위치가 반영됐다(블롭 서식 쓰기 회피)
6. **검증기도 검증한다** — 일부러 오류를 심어 검사기가 잡는지 확인. 두 번은 "통과만 하는 가짜 검사"였다

## 미완 (재개하면 여기서)
- 2판 개정 5~7단계: 단어 강조(도너에서 "한 줄에 색 둘"인 큐 31개 특정, 색 필드 바이트 대조부터) · 효과음 도입 · 밀도 3종(컷 2.4배·나레 2.2배·발화 0.99가 지무비 기준)
- 장면이해 단계(제미나이가 영화 전체 시청 — EvoLink + 유튜브 URL 자동화는 이미 성공)
- 프리셋 확장(뇌전구 등 숏폼) · 유스_클로닝 보이스 · render(mp4 자동)

## 운영 메모
- 키: 레지스트리만. 노출 3건(GROQ·EVOLINK·SPEECHMATICS)은 **교체 대기(시기 미정)** — 운영 중이라 폐기 금지, 새 키 발급→반영→확인→옛 키만 무효화
- 외부 배포(지인) 전엔 사용자별 토큰 필요(작업량 반나절 추정)
- 주간 하네스 정비 리마인더(금요일 10시 카톡) 예약 유지 중 — 프로젝트를 접으면 삭제할 것
- 경로: 저장소 `Desktop\youstudio-mcp\`(274MB, GitHub private 동기) · 산출물 `Desktop\youstudio_work\fulltime\`(59MB) · 참고(읽기 전용) `Desktop\볼케이노 MCP\`·`가족전달\`

---
---

# 2. 볼케이노-린박스-prproj-작업일지.md — 종료 보고

> 로컬에도 사본 있음: `Desktop\볼케이노 MCP\린박스_불륜문제6화\인수인계.md`

**상태: 종료.** 린박스 01편 자막 74장을 프리미어 텍스트 그래픽으로 심는 작업. 74장 주입·열기까지 성공했고 가로 비율과 좌표 두 가지를 남기고 접었다.

## 확정 사실

**견본은 스타일에 안 매인 것이어야 한다** — 도너 자막 53개 중 자립형은 `item 708` 하나뿐(계보 41블록). 745(대사, 65블록·불륜기본 스타일)·759(효과, 65블록·불륜2 스타일)는 복제하면 verify 33항목을 통과하고도 프리미어가 거부한다. 스타일 계보를 통째 복제하는 길도 별도로 실패했다.

**서식 패치 3종** — 색은 StyleTable 색 필드 **3바이트 제자리 쓰기**(vtable로 위치 특정, 블롭 길이 불변). 크기는 float32 제자리 쓰기. 글꼴은 문자열이라 **tail relocation**(끝에 4바이트 정렬로 붙이고 uoffset 하나만 이동). 바이트 패턴을 1바이트씩 훑어 덮어쓰는 것은 절대 금지 — `0xFFFFFF`는 `-1` 안에 널려 있어 남의 데이터를 뭉갠다.

**색 채널 생략 규칙** — 색 미니테이블은 값이 255인 채널의 필드를 아예 만들지 않는다. 흰색(255,255,255)은 세 채널 전부 생략되므로 없는 필드를 0으로 읽으면 `[0,0,0]`이 나온다. 필드가 없는 런은 임의 색을 못 받는다. (영화롱폼의 "흰 자막인데 색이 [0,0,0]으로 읽힌다"도 이걸로 설명된다)

**좌표 규칙** — 정렬 버튼 왕복 실측: 85px (540.47, 982.86) · 70px (540.38, 978.83). `22.86/85 = 0.26894`, `18.83/70 = 0.26900`.
```
텍스트 위치   = ( 0.5 , (960 + 0.269 × 글자크기) / 1920 )
벡터모션 위치 = ( X / 1080 , Y / 1920 )
```
텍스트 기준점(0:0)은 건드리지 않는다. 가로 비율은 도너 자막 52개가 100, 708 헤드라인만 124.98.

**방법론 — 사람이 값을 먹이고 우리가 읽는다** — 파일 형식을 모를 때 가장 빠른 길은 프리미어에 값을 입력·정렬시키고 그 결과를 파일에서 읽는 것. 색·좌표·정렬 세 번 다 이 방법으로 풀렸다. 화면을 픽셀로 재려 하면 오차 때문에 겉돈다.

**검사와 그 한계** — `verify()` 33항목 + MasterClip 유일성 게이트 + 계보 블록 수 게이트. **셋 다 통과해도 프리미어가 거부할 수 있다(실제로 두 번).** 파일 값이 맞아도 화면 자리는 틀릴 수 있다(좌표·가로 비율 오류 둘 다 사람 눈이 잡음). 매 단계 실제로 열고 눈으로 본다.

**부수 사실** — `BinaryHash = uuid4()[:28] + f"{블롭길이+12:08x}"` · ObjectID는 한 줄로 매긴다 · 한국어판은 파라미터 이름이 다르다(`소스 텍스트`/`Source Text`, `위치`/`Position`) · `Cafe24Ohsquare-v2.0.otf` 하나가 PostScript `Cafe24OhsquareOTF` + 현지화 `카페24 아네모네 OTF`를 갖고 **libass는 현지화 이름을 못 찾는다**(ASS·ffmpeg엔 영문 이름) · 도너를 프리미어에서 재저장해도 폰트 벡터는 안 바뀐다.

**죽은 가설** — ObjectID 3500 천장 · 그래픽 개수 한계 · MasterClip 공유 · 좌표 편집 · 텍스트 기준점 0.5:0.5. 전부 반증됨.

## 이 트랙의 다른 자산
- **구간 절단은 `-i` 뒤 `-ss` + 재인코딩.** `-c copy`는 키프레임에서만 잘려 `video start_time=0.806`이 생기고 앞 블록 싱크가 350ms 깨진다.
- **`_synccheck.py` 3단계**: ①구간 v/a start 정렬 ②블록별 영상·소리 길이차 0.08초 이내 ③완성본 D블록 원음 포락선 상관, 시프트 100ms 이내.
- **훑기(EvoLink)는 `--from/--to`로 반씩.** 56분 통째는 프록시 18.29MB로 인라인 한도 초과.
- **린박스 규격**: 편 47~59초 · 컷 중앙 1.3초 · D블록 1.2~1.5초 · N:D 1:2 이상 · 한 대목 206초 · 크롭 `376:668:772:87` · 효과자막 팔레트 8색 · 편집 효과음 없음.
- **템플릿 규격**: 헤드라인 Gmarket Sans Bold 130(ffmpeg)/91(prproj) · 대사 Cafe24 Ohsquare OTF 150/70 · 효과 170/85 · 매트 상단 y0~453 하단 y1823~1920 · 효과자막 안전대 y520~1140.

## 산출물
`완성_01.mp4` — 1080×1920 · 55.2초 · 블록 31 · 효과자막 15 · 헤드라인 "집에 들어온 / 낯선 남자의 정체" · 성우 Typecast Ina. 싱크 검사 3단계 통과. 02~05편 미착수, prproj 미완.

---
---

# 3. 볼케이노-린박스-운영규칙.md — 소통 규칙

> 로컬에도 사본 있음: `Desktop\볼케이노 MCP\소통규칙_개정판.md`

01편이 5번 재작업됐고 **그중 4번을 사람이 눈으로 발견**했다. 완성 판정을 코드로 내리는 것이 핵심.

0. **옆에 이미 있는 것부터 확인** — 새 기술 문제에 맨땅에서 파기 전에 옆 저장소를 본다
1. **지어내지 않는다** — 제목·원제·이름·회차는 소재·지침서·사람에게서만. 없으면 멈추고 묻는다
2. **규격을 확장 해석하지 않는다** — "원제 verbatim [윗줄,아랫줄]"에 회차를 붙이지 않는다
3. **이상 신호는 그 단계에서 처리** — 전사에서 무음 140초가 드러났는데 다음 단계로 밀어 한 편을 버렸다
4. **한 번에 변수 하나만** — 글자·글꼴·크기·색을 동시에 바꿔 하루를 태웠다
5. **한글 이스케이프 금지 · heredoc 금지** — "슬짝/슬근/배응" 오타, 스크립트가 성공 보고하고 파일엔 미반영
6. **패치 후 되읽어 확인** — 스크립트 성공 메시지를 믿지 않는다
7. **확인 vs 짐작을 문장마다 구분**
8. **2시간 초과 시 강제 보고** · 같은 목표에 접근법 3개 실패 시 별도 트랙 분리
9. **설치·레지스트리·권한상승·사용자 원본 열기는 사전 승인**
10. **유료 API 단계 전 예상 비용 고지**

### 완성 보고 게이트 10개
`_synccheck.py` 3단계 / 길이 47~59초 / 상하단 매트 존재 / 헤드라인 2줄 지침서 준수 / 효과자막 y 520~1140 / 글꼴 폴백 없음 / 해상도 1080×1920 / `captions.ass` sha256 서버 일치 / 마무리 블록 배경에 인물 / 전 자막 낭독 오타 점검. prproj는 `verify()` 통과 추가.

### 볼케이노 MCP 현황 (참고)
주소 `https://volcano-mcp.groove1027.workers.dev` · Bearer 토큰 필요 · 프리셋 6개(린박스·명화관cinema·뇌전구·웃긴버거·어랍숏·인물형) · 도구 `volcano_video`(33단계)·`volcano_cardnews` · **프리미어/prproj 단계는 서버에 없음**(mp4까지가 끝) · `textutil` 부재로 docx/rtf 못 읽음 · ⚠ 권한 불안정(채널 목록이 4개↔6개를 오감, 재연결로 복구)

---
---

# 4. 15일-로드맵-배포판-MCP.md (2026-08-14 · 당시 최신 계획)

## 목표
남에게 **배포하는** 원격 MCP 서버 `youtube-craft`. TypeScript + Cloudflare Workers 직행, 서버 역할 = 제작 절차 도구화, 배포 범위 = 지인.

## 핵심 개념 — 도구의 세 종류
- ❌ **일반 지식** — "좋은 대본 쓰는 법". AI가 이미 앎. 도구로 감싸면 오히려 나빠짐
- ✅ **규격 전달형** — "이 채널은 HOOK 30초 / 문장 20자 / '입니다' 금지". AI는 알 방법이 없음
- ✅ **검사·판정형** — "20자 넘는 문장 몇 개?". **AI는 세기를 못하고 매번 답이 다름. 코드는 항상 같은 답**

**서버의 진짜 가치는 코드가 아니라 `RULES` 상수.** 코드는 Claude Code가 써주지만 기준은 아무도 대신 못 만듦.

## 서버 명세 `youtube-craft`
`get_script_blueprint(duration_min)` 규격전달 / `check_script(script)` 검사 / `diagnose_metrics(ctr, retention)` 판정 / `get_packaging_rules()` 규격전달 / `/새영상기획` 프롬프트

## 기술 사실 (2026-08-14 실행 검증)
- 패키지 **`@modelcontextprotocol/server` 2.0.0** (`@modelcontextprotocol/sdk` 아님), zod 4.x 자동 포함
- `import { McpServer, createMcpHandler } from "@modelcontextprotocol/server"` / `import * as z from "zod/v4"`
- `new McpServer({name, version})` → `server.registerTool(name, {title, description, inputSchema: z.object({...})}, async (args) => ({content:[{type:"text",text}]}))`
- `const handler = createMcpHandler(() => buildServer())` → `export default { fetch: (req) => handler.fetch(req) }`
- **Workers 지원 내장** — `nodejs_compat` 불필요
- 스캐폴드 `npm create cloudflare@latest` / 배포 `npx wrangler deploy` / 로컬 `npx wrangler dev`
- 주소 형태 `https://<worker>.<account>.workers.dev` (경우에 따라 `/mcp` 필요)
- Cloudflare 공식: **`McpAgent` 패턴은 deprecated**, 신규는 `createMcpHandler`

## 원칙
30분 규칙(같은 에러에 30분 이상 금지) · 도구 4개 중 3개만 돌아도 서버는 쓸모 있음 · D8에 배포까지 가는 것이 중요(인터넷 주소를 갖는 경험이 나머지를 끌고 감) · 인증이 없으면 서버에 비밀을 넣지 말 것

## Sources
- Build a Remote MCP server · Cloudflare Agents docs
- typescript-sdk/docs/server.md
- Connect to MCP servers — Claude Code Docs

---
---

# 5. 15일-로드맵-MCP-에디션.md (2026-08-14 · 폐기된 이전 버전)

> 로컬 stdio 파이썬 서버 계획. 배포판(문서 4)으로 대체됨.

**전제** — 『지능의 레버리지』에 MCP 제작 방법은 없다(부록 4장 p.449~450뿐). 그러나 **Part 2-4장 「하네스 엔지니어링」(p.122~146)이 사실상 MCP 설계 사양서.**

| 하네스 4요소 | 노션 구현 | MCP |
|---|---|---|
| ① 컨텍스트 관리 | 노션 DB | `get_channel_context()` |
| ② 도구 오케스트레이션 | 커스텀 에이전트 | **Tools — MCP의 본체** |
| ③ 에러 복구 | 지침에 검증 규칙 | try/except + 읽을 수 있는 에러 문장 |
| ④ 상태 지속성 | 노션 DB | JSON 파일 |

**15일 안에 되는 것/안 되는 것** — 로컬 파일 기반 도구 ✅ / YouTube Data API v3(API 키만) ✅ 할당량 10,000유닛/일 / ⚠ CTR·평균 시청 지속 시간 = Analytics API = **OAuth 2.0 필수**

**서버 `youtube-studio`** — `get_channel_context` · `list_ideas` · `add_idea` · `save_script` · `get_script` · `get_video_stats`
**핵심 설계 원칙: AI가 스스로 못 하는 것만 도구로.** 도구는 손이지 머리가 아님.

**기술 사실** — MCP Python SDK 2.0.0, Python 3.10+ / `from mcp.server import MCPServer` → `@mcp.tool()` → `mcp.run(transport="stdio")` / `uv init`·`uv add "mcp[cli]"` / Windows 설정 `%APPDATA%\Claude\claude_desktop_config.json` → 앱 완전 재시작 / `claude mcp add 이름 -- uv --directory /절대경로 run server.py` / ⚠ **stdio 서버에서 `print()` 금지**(통신 통로 오염), `logging` 사용

---
---

# 6. 15일-학습-로드맵.md (2026-08-14 · 최초 버전)

> 『지능의 레버리지』 학습 계획. MCP 목표가 정해지기 전 버전.

**설계 원칙** — 484p 중 정독 대상 약 210p. "다 읽기"가 아니라 **7개 산출물 만들기**가 목표. 책의 방법론(OKR·ICE·울트라디언 90/20·KPT)을 학습 계획 자체에 적용.

**최종 산출물 7가지** — ①컨텍스트 자산 .md ②Gem 2개 + 클로드 Project ③노션 유튜브 하네스 ④채널 전략 1페이지 ⑤영상 1편 + 숏폼 3개 ⑥주간 사이클 + AI 주간 리포트 ⑦수익화 씨앗 1개

**지표 밴드** — CTR: 10%+ 급확대 / 5~10% 점진 / 2~5% 유지 / 2%- 축소. 시청 지속률: 70%+ / 50~70% / 30~50% / 30%-
**NDA 스코어카드** 15점 만점, 12점 이상 유망. **필라 배분** 교육60·실험20·경험10·트렌드10.
**AI 초안 70% + 내 경험·말투 30%** 3단계 변환(문체 전환/경험 주입/연출 지시).

**하지 말 것** — 온라인 강의(구독 5,000+ 이후) / 플랫폼 3개 이상 / 웨비나·자체 웹사이트 / 여러 상품 동시 제작 / 수익 조급증(애드센스는 구독 1만·월 조회 10만에도 월 30~50만원. 순서는 가치→신뢰→거래)

> "의지력은 3일, 시스템은 3년 간다."
> 포기해야 한다면 **읽기를 포기하고 만들기를 남길 것.**
