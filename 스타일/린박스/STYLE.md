# 스타일 · 린박스 — 목차 (초안 2026-09-03 맥2)

**드라마 로컬 영상(화이트리스트)의 한 대목을 잘라 40~60초 숏폼을 만든다.** 원음 대사(D) 블록과 TTS 나레(N) 블록을 엮고, 위 매트에 2줄 제목·아래 매트에 로고/문구를 얹어 **완성 mp4 와 프리미어 prproj 를 함께** 낸다.
규격 정본은 볼트 `스크립트/린박스/키트/규격.md`(§93 까지)와 카드 `프리셋/린박스.md` 이고, 2026-09-03 윈도우 직원 키트와 합쳐 승격한 키트가 이식 원본이다.

숫자는 세 파일에만 있다. `.md` 는 가리키기만 한다.

| 파일 | 내용 |
|---|---|
| [정답지.json](정답지.json) | 게이트 합격 대역 — 착수(쓸거리·밀도·하단·편정보) · 구조(길이·컷분·번쩍임·장면튐·완료표시) · 자막(제목·구두점·폭·카드·말머리·화자·자리·글꼴) · 소리(싱크·라우드니스·prproj 마스터) · 화면 · 프리미어 · 사람확인 |
| [규격.json](규격.json) | 우리가 정한 설정값 — 매트 3층 · 재프레이밍 · 제목/자막/모션자막 서식 · 하단 · 편집 호흡 · 나레(이나 1.2배속) · 소리(−14.0 LUFS) · 전사 · 산출물 · 프리미어 사슬 |
| [우리실측.json](우리실측.json) | 잰 물성 — TTS 10.2자/초 · 라우드니스 실측 · 마스터 밀림 · 글꼴 환산비 · 서버 배율 널뜀 · 튐 기여 · 싱크 드리프트 |
| [대본.md](대본.md) | 계획·집필 지침 — 쓰기 전 세 가지(쓸거리·밀도·화자) · 하단/제목은 묻는다 · 한 통으로 결말까지 · 블록 대본 꼴 · `\|` 카드 경계 · 굽기 전 검사 |

★상태: **초안.** styles.ts 등록·`lb_*` 처리기는 다음 세션. 키트의 make_ass·drv2·render·export_premiere·번쩍임정리·장면튐검사·검증 7파일은 윈도우 취사병 1편 재현 통과 전엔 확정이 아니라, 거기서 온 값은 `_미검증` 으로 표시했다(진행상황.md 2026-09-03).
★설계안 `설계/프리셋_린박스.md` 는 2026-09-03 현재 저장소·볼트 어디에도 없다 — 아래 단계표는 그 문서 2절이 오면 «확정» 근거로 붙인다.

## 제작 단계 — 볼트 키트 `한편.py` 실측 (2026-09-03 · 합쳐 승격한 판에서 읽음)

`한편.py` 는 세 단계다: **① 준비 → (사람: 대본 author.py) → ② 굽기 → ③ 프리미어.** 아래는 코드가 실제로 부르는 차례다(«꼭» = 실패하면 멈춤 · «선택» = 실패해도 진행). 맥의 신병4·불륜 사슬은 서버 ass 를 받은 뒤 `신병4/한번에.sh`(서식.py → 그래픽·계획 → xml짓기 → 프리미어 → 곳간 서식 → 완성굽기)로 가므로 ②의 make_ass·render·export_premiere 를 타지 않는다 — 두 경로를 모두 적는다.

### ① 준비 — `한편.py 준비 <편폴더> --소재 <드라마.mp4> --시작 s --끝 e [--자막 srt --시각차]`

| 순 | 도구 | 하는 일 | 산출물 | 요금 |
|---|---|---|---|---|
| 1 | `새편.py` | 편 폴더 차리기 — 도구·자산(runner·fonts·sfx_pick·yunet) 복사(있는 파일은 안 덮음) | 편 폴더 | |
| 2 | ffmpeg | 구간 자르기(`-ss/-to` + 재인코딩 crf16 · aac 320k) · 손대지 않은 사본 `구간_원본.mp4`(§84) · stream start_time > 0.1 이면 옛 컷 표 폐기(§78) | `구간.mp4` `구간_원본.mp4` | |
| 3 | (한편.py) | 소재 크기 재서 `find_faces.py`·`reframe.py` 의 WIN 을 소재높이×1080/1020 로 | | |
| 4 | `전사.py` | Speechmatics 전사(낱말사전 적용) → 대사.json · 대사표.txt | `대사.json` `대사표.txt` | ★Speechmatics |
| 5 | `장면컷.py 구간.mp4 --쓰기` | 장면전환 표 — 이 파이프라인의 뿌리(§71 ffmpeg select 금지) | `scene_cuts.txt` | |
| 6 | `find_faces.py` | 촬영본마다 얼굴(프레임별 최대 얼굴의 중앙값) | `faces.json` | |
| 7 | `인물따라가기.py --쓰기` (선택) | 한 사람이 촬영본 안에서 움직여 벗어나는 자리 (ease 0.35) | | |
| 8 | `reframe.py` | 인물을 창 가운데로 → 세로 1080×1020 | `구간_인물.mp4` | |
| 9 | (`--자막` 있을 때) `뽑기` | 공식 SRT 를 구간 시각으로 옮겨 적음 | `_공식자막.txt` | |
| — | **사람/클로드** | `author.py` 대본(대본.md) · 하단 묻기 · 제목 후보 | `authored.json` | |

규격 §8 의 «2b 화자표.py · 4 화자추적.py · 5b 나레카드.py · 5c 편검사.py» 는 `한편.py 준비` 가 부르지 않는다(대본.md 가 사람에게 시킨다 / 굽기의 편별검사·make_ass 가 대신한다) — 설계안이 단계로 둘지 정한다.

### ② 굽기 — `한편.py 굽기 <편폴더> [--완성본 이름.mp4]`

| 순 | 도구 | 하는 일 | 꼭 | 요금 |
|---|---|---|---|---|
| 1 | `도구/쓸거리검사.py .` | 안 겹치는 말 ≥ 31초 → 아니면 **멈춤**(§83) | stop | |
| 2 | `author.py` (authored.json 없을 때) | 블록 대본 → authored.json · extra_payload.json | | |
| 3 | `도구/도구맞추기.py . --쓰기` | 편 폴더 도구를 키트와 맞춤(§73) | 선택 | |
| 4 | `나레자리.py --쓰기` [`--이어받기 --여유 1.75`] | 나레 정지컷 자리(원본 자막 쓰는 편은 앞 대사 끝에) | 선택 | |
| 5 | `전환맞추기.py --쓰기` ×2 | 블록 경계를 장면전환에 | 선택 | |
| 6 | (한편.py) | blocks/ 의 bNN·sig·master·merged 지움 · 소재 프레임률 재서 `프레임률` 파일(§82) | | |
| 7 | `drv2.py stitch_source|stitch_narr 40` | **볼케이노 서버 사슬** — TTS 합성·블록 굽기(훅: 고아블록치우기·묵은것깨기·배율맞춤·프레임률맞춤·배율상한·화질). 반려면 stitch_narr 로 한 번 재시도 | 선택 | ★서버(EvoLink·Typecast) |
| 8 | `서버낱말.py` | 서버가 들은 낱말 → src_words.json | 선택 | |
| 9 | `대체화면.py` · `speed_narr.py`(꼭) · `align_narr.py` · `narr_align.py` · `d_sync.py` · `make_ass.py`(꼭) | 나레 배속·낱말 시각 재기·원음 카드 싱크·자막 ass | speed_narr·make_ass 꼭 | |
| 10 | `도구/자막말머리맞춤.py --쓰기` | 첫 카드를 말머리에(§93) | 선택 | |
| 11 | `add_sfx.py` · `라우드니스.py`(꼭) | 효과음 믹스 · 2패스 −14.0 LUFS | 라우드니스 꼭 | |
| 12 | `도구/편별검사.py` | 본보기 찌꺼기 → 있으면 **멈춤**(§86) | stop | |
| 13 | `도구/컷잦음검사.py` · `장면튐검사.py` | 알림 / 구운 블록으로 튐 판정 | 선택 | |
| 14 | `render.py` | 완성 mp4 (굽기 전 관문 대본검사·장면튐검사·제목검사 자동) | 선택 | |
| 15 | `도구/튐분석.py . <완성본>` | 나온 그림의 튐 실측 | 선택 | |

### ③ 프리미어 — `한편.py 프리미어 <편폴더> [--곳간 <이름>]` → `프리미어내기.py`

| 순 | 도구 | 하는 일 |
|---|---|---|
| 1 | `자막나누기.py` | ass 를 층(템플릿·나레·대사·모션)으로 |
| 2 | `export_premiere.py --자막MOV없이` | 원본 소재 + 컷 타임코드 + 텍스트 자막 → FCP7 XML · 템플릿 MOV · 자막층.json |
| 3 | `프로젝트뽑기.py <편> --기다림` | 대기줄에 올리고 프리미어(CEP 확장 auto_prproj.jsx)가 prproj 를 만들 때까지 기다림(§1.1 «내가 뽑는다») |
| 4 | `자리잡기.py` | ass 자리를 프리미어 값으로 |
| 5 | `본떠서만들기.py <곳간>` | 곳간 서식(스타일/<곳간>_본.json) 입힘 |
| 6 | `꾸미기.py` | 팝 등 |
| 7 | `모션자막살리기.py` | 모션자막 자리·기울기·제목자막 층 |
| 8 | `아모르입히기.py --본` | 아모르 팝(스타일/아모르_부품.prproj — 보류 중인 두 부품 결정 대기) |
| 9 | `주입검사.py` · `자막대조.py` · `자막자리대조.py` | 검사 셋 |

맥 신병4/불륜 경로(`신병4/한번에.sh <편> <마스터>`)는 위 ③을 «서식.py → 폭맞춤·ass자리검사·구둣점검사 → 그래픽짓기·계획짓기·자막끝맞춤·원음스템 → xml짓기 → 프리미어(잠금) → 본떠서만들기·서식입히기·자리잡기·꾸미기·가로비율·그림자빼기 → 주입검사·자막자리검사 → 마스터효과심기 → 아모르입히기 → 제목끝맞춤 → 영상굽기·마스터맞춤·add_sfx·라우드니스·완성굽기·대조점짓기 → _synccheck» 로 대신한다. 이식 단계(lb_*)를 정할 때 **어느 경로를 정본으로 둘지**가 설계안의 결정 사항이다.

### 요금이 드는 자리 (실행 전 예상 비용을 말하고 승인)
전사(Speechmatics) · 서버 사슬(볼케이노: TTS Typecast 이나 + EvoLink). 나머지는 로컬 ffmpeg·프리미어.
