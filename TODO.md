# TODO — 잊으면 안 되는 것 (프로젝트 문서 정리 후 여기로 옮김 · 2026-08-27)

## 🔑 노출된 API 키 3건 — 아직 교체 안 됨
| 키 | 노출 | 상태 |
|---|---|---|
| `GROQ_API_KEY` | ~2026-08-15 claude.ai 채팅 | **교체 대기** |
| `EVOLINK_API_KEY` | ~2026-08-15 claude.ai 채팅 | **교체 대기** |
| `SPEECHMATICS_API_KEY` | 2026-08-17 Claude Code 대화 로그 | **교체 대기** |

서버가 운영 중이라 **옛 키를 그냥 폐기하면 안 된다.** 순서(`설계/외부서비스.md` 보안 체크리스트):
1. 서비스 대시보드에서 **새 키 발급**
2. 각 컴퓨터 `~/.volcano/keys/<이름>` (또는 환경변수)에 **반영** — 서버는 키를 갖지 않으므로 서버 쪽은 손댈 것 없음
3. 새 키로 한 번 호출해 **200 확인**
4. **그 뒤에** 옛 키만 무효화 → 옛 키로 401 확인
언제 할지: 미정 (사장님 결정). 끝나면 이 절을 지우고 `설계/외부서비스.md` 3) 표를 「교체 완료 <날짜>」로.

## 📦 두 번째 프리셋을 붙일 때 선행 작업
- 서버를 파이프라인 등록표(프리셋마다 단계 순서)로 전환 — 지금은 `styles.ts` 규격 갈아끼우기뿐
- 외부 배포 전 사용자별 토큰 (지금은 `YOUSTUDIO_TOKEN` 하나를 3대가 공유)
자세히: 볼트 `프리셋/유스튜디오_영화롱폼.md` 4절

## 📚 옛 프로젝트 문서
claude.ai 프로젝트 「유스튜디오 MCP 구축」의 문서 6개는 `_프로젝트문서_백업_20260827.md` 로 묶어 저장소에 보관.
