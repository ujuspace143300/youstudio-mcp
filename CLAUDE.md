# CLAUDE.md — 영화 롱폼 리캡 MCP

이 파일은 목차다. 상세는 각 문서에 있다. **100줄을 넘기지 마라.**

## 이 프로젝트는

영화 한 편을 **롱폼 리캡 영상**으로 만드는 나만의 MCP 서버.
사람이 방향을 정하고, 클로드가 실행한다.

## 나에 대해

- 나는 **비개발자**다. 코드를 설명할 때 평범한 말로 해라. 전문 용어를 쓰면 한 줄로 풀어 써라.
- 지침·기준·판단은 전부 파일에 적는다. 적히지 않은 것은 없는 것이다.

## 반드시 따를 것

- **`HARNESS.md` 의 원칙을 항상 따른다.** 목차형 문서, 조각 분리, 방법이 아닌 조건, 숫자 반환, 수리 지침 붙은 게이트.
- 문서와 실제가 어긋나면 문서를 고친다.
- **의미 있는 변경이 끝나면 커밋한다.** 커밋 메시지는 무엇을 왜 바꿨는지 한국어 한 줄.
- **파이프라인 단계 하나가 실호출까지 통과하면 마일스톤 태그를 단다** (`v0.2-script` 식으로). 태그 메시지는 무엇을 완주했는지 한 줄.

## 참고 자료

- `C:\Users\user\Desktop\볼케이노 MCP` — 기존 숏폼 파이프라인. **읽기만 한다. 수정 금지.**
- 거기서 배운 것은 `설계/참고자료분석.md` 에 적는다.

## 폴더 구조

```
youstudio-mcp/
├── HARNESS.md              설계 원칙 (목차)
├── README.md               GitHub 대문 — 프로젝트 소개·구조 지도·파이프라인 현황
├── CLAUDE.md               이 파일
├── 서버/                   MCP 서버 (TypeScript · Cloudflare Workers). 실행·테스트법은 서버/README.md
├── 설계/
│   ├── 단계와게이트.md       범위·판정 위치·대역 체계·확장 계획 (단계 목록은 단계상세.md 를 가리킴)
│   ├── 단계상세.md          8단계 명세 — 입력·출력·지표(metrics)·게이트·수리 지침
│   ├── 참고자료분석.md       참고 분석 목차 + 가져올 것 / 버릴 것
│   ├── 참고_runner.md        볼케이노 runner 구조
│   ├── 참고_40단계.md        롱폼 파이프라인 40단계
│   ├── 참고_게이트.md        게이트와 대역
│   ├── 참고_대본지침.md      가족 대본 프롬프트 요지 — 문체·길이·유머·인물 호칭·스포 어휘 (나레이션.md 재료)
│   ├── 참고_export.md        가족 프리미어 XML 3종 = FCP XML v5 — 우리 export 형식 근거
│   ├── 가족인터뷰.md          가족 강제 규칙(제미나이가 영화 전체를 본다)·검증 프로세스 원문 + 물어볼 것 Q1~5
│   ├── 장면이해_설계안.md      새 단계(제미나이 전체 시청) 설계안 — 결정 대기, 구현 전
│   ├── prproj생성_설계안.md    본선 산출물 .prproj 이행 계획(계단 0~4, 도너 절차서, 위험·검증·되돌아갈 지점) — 구현 전
│   └── 참고_prproj구조.md     계단 0 학습 — prproj 오브젝트 그래프·틱·컷/오디오/나레/자막(블롭) 매핑 표·검증 규칙
│   └── 외부서비스.md         외부 API 목록 — 용도·단계·환경변수 이름·상태·결정할 것. 키 원칙
├── 벤치마크/               가족 완성본 실측 수치 (볼케이노_FullTime_실측.json) — 우리 결과와 나란히 잴 기준
├── 도너/                   우리 도너 prproj(사용자가 프리미어에서 만듦)와 절차서.md — 산출물의 뼈대, 규격의 일부
└── 스타일/
    └── 영화롱폼/
        ├── STYLE.md          얇은 목차. 옆 파일들을 가리키기만 함
        ├── 정답지.json       게이트 합격 대역(지무비 19편). 목표
        ├── 규격.json         우리가 정한 설정값. 스타일 조정 때 바뀜
        ├── 우리실측.json     우리 엔진 물성. 영상마다 갱신
        ├── 구간선택.md        어느 장면을 쓸 것인가
        ├── 나레이션.md        집필 지침
        └── 자막.md           자막 규칙
```

## 문서 읽는 규칙

- 단계마다 **필요한 문서만** 읽는다. 대본 쓸 때 자막 규칙까지 읽지 않는다.
- 숫자는 `정답지.json`(목표) · `규격.json`(설정) · `우리실측.json`(결과) 세 파일에만 둔다. `.md` 는 숫자를 가리키기만 한다. 고칠 수 있는 것은 규격뿐이다.
