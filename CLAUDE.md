# CLAUDE.md — 영화 롱폼 리캡 MCP

이 파일은 목차다. 상세는 각 문서에 있다. **100줄을 넘기지 마라.**

## 이 프로젝트는

영화 한 편을 **롱폼 리캡 영상**으로 만드는 나만의 MCP 서버.
사람이 방향을 정하고, 클로드가 실행한다.

## 나에 대해

- 나는 **비개발자**다. 코드를 설명할 때 평범한 말로 해라. 전문 용어를 쓰면 한 줄로 풀어 써라.
- 지침·기준·판단은 전부 파일에 적는다. 적히지 않은 것은 없는 것이다.

## 반드시 따를 것

- **`HARNESS.md` 의 원칙을 항상 따른다.** 목차형 문서, 조각 분리, 방법이 아닌 조건, 숫자 반환, 수리 지침 붙은 게이트.
- 문서와 실제가 어긋나면 문서를 고친다.
- **의미 있는 변경이 끝나면 커밋한다.** 커밋 메시지는 무엇을 왜 바꿨는지 한국어 한 줄.

## 참고 자료

- `C:\Users\user\Desktop\볼케이노 MCP` — 기존 숏폼 파이프라인. **읽기만 한다. 수정 금지.**
- 거기서 배운 것은 `설계/참고자료분석.md` 에 적는다.

## 폴더 구조

```
youstudio-mcp/
├── HARNESS.md              설계 원칙 (목차)
├── CLAUDE.md               이 파일
├── 서버/                   MCP 서버 (TypeScript · Cloudflare Workers). 실행·테스트법은 서버/README.md
├── 설계/
│   ├── 단계와게이트.md       파이프라인 단계, 단계별 지표, 게이트 조건과 수리 지침
│   ├── 참고자료분석.md       참고 분석 목차 + 가져올 것 / 버릴 것
│   ├── 참고_runner.md        볼케이노 runner 구조
│   ├── 참고_40단계.md        롱폼 파이프라인 40단계
│   └── 참고_게이트.md        게이트와 대역
└── 스타일/
    └── 영화롱폼/
        ├── STYLE.md          얇은 목차. 옆 파일들을 가리키기만 함
        ├── 정답지.json       게이트 합격 대역(지무비 19편). 목표
        ├── 규격.json         우리가 정한 설정값. 스타일 조정 때 바뀜
        ├── 우리실측.json     우리 엔진 물성. 영상마다 갱신
        ├── 구간선택.md        어느 장면을 쓸 것인가
        ├── 나레이션.md        집필 지침
        └── 자막.md           자막 규칙
```

## 문서 읽는 규칙

- 단계마다 **필요한 문서만** 읽는다. 대본 쓸 때 자막 규칙까지 읽지 않는다.
- 숫자는 `정답지.json`(목표) · `규격.json`(설정) · `우리실측.json`(결과) 세 파일에만 둔다. `.md` 는 숫자를 가리키기만 한다. 고칠 수 있는 것은 규격뿐이다.
