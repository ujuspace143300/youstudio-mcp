# 외부서비스.md — 앞으로 쓸 외부 API 목록

> 우리 파이프라인이 밖에 부탁하는 것들. 어느 단계가 무엇을 어느 키로 쓰는지 한 곳에 둔다.
> 값(모델·보이스·설정)은 `스타일/영화롱폼/규격.json` 에, 호출 규약은 `참고_runner.md` 에. 여기는 **목록과 결정할 것**만.

## 공통 원칙

- **키는 전부 Windows 사용자 환경변수만.** 파일에 쓰지 않는다. (`[Environment]::SetEnvironmentVariable('<이름>','<키>','User')` — 새 터미널부터 보인다)
- **서버 무보관.** 서버 응답의 `auth` 는 `{env:"<환경변수 이름>"}` 으로 위치만 지시하고, runner 가 로컬에서 읽어 헤더에 붙인다. 응답·payload·로그·파일에 키 값이 나오면 안 된다.
- **커밋 전 키 검색 0건 확인.** 저장소(추적·비추적)와 작업 폴더에서 키 문자열을 찾아 0건이어야 커밋한다.
- **계정은 각자 발급이 원칙.** 가족 키를 쓸 경우 청구가 그쪽으로 가므로 **사전 합의** 필요. 어느 계정 키인지 아래 표에 적는다.

## 목록

| 서비스 | 용도 | 우리 단계 | 환경변수 | 상태 | 결정할 것 |
| :---- | :---- | :---- | :---- | :---- | :---- |
| **EvoLink** | 텍스트 판정 (Gemini Google-Native v1beta 리셀러) | brief · (script 심사 후보) | `EVOLINK_API_KEY` | **사용중(진행중)** — brief 실호출 통과 2026-08-15. 모델 gemini-3.5-flash | 영상 판정은 못 한다(Files API 없음) → select 는 Google 순정 |
| **Groq** | 전사 (whisper-large-v3-turbo) | transcript | `GROQ_API_KEY` | **사용중** — Full Time 205발화 2026-08-15 | 장편 분할 전사(파일 상한 25MB 초과분) |
| **Google Gemini (순정)** | 영상 판정 — Files API 업로드 + 화면 보기 | select | `GOOGLE_API_KEY` | 대기 | 키 발급 · 조각(클립) 크기 · 업로드 상한 |
| **Speechmatics** | 전사 폴백 1순위 | transcript | `SPEECHMATICS_API_KEY` | 대기 | Groq 실패 조건(무엇이면 폴백하나) · 응답 스키마 → transcript.json 변환 |
| **Gladia** | 화자 분리 | transcript 보강 | `GLADIA_API_KEY` | 대기 | **화자 구분 자막이 필요한 시점** — 그 전엔 붙이지 않는다 |
| **ElevenLabs** | 나레이션 TTS (eleven_v3) | voice | `ELEVENLABS_API_KEY` | 대기 | **보이스 ID** (가족 계정 자산인지 확인 — 자산이면 청구·권리 합의) · Typecast 와 택일 또는 병행 |
| **Typecast** | TTS 한국어 보이스 | voice 의 선택지 | `TYPECAST_API_KEY` | 대기 | ElevenLabs 와 택일 또는 병행 · 보이스 선택 |

(EvoLink·Speechmatics·Gladia·ElevenLabs·Typecast 는 지시받은 목록. Groq·Google 순정은 이미 쓰거나 결정된 것이라 같이 적었다.)

## TTS 를 정할 때 반드시

★ **보이스를 정하면 나레이션 길이·호흡이 달라진다.** 그러므로 TTS 관련 대역(자당 초·문장당 길이·블록 사이 숨 등)은 정답지를 그대로 못 쓰고 **우리실측.json 으로 다시 재야 한다.** 근거: 가족 파이프라인 README — TTS 실측 길이(`char_end`)가 곧 타임라인 슬롯이며(measure-never-predict), 보이스가 바뀌면 그 실측이 전부 바뀐다. `단계와게이트.md` 「우리가 재야 할 것」 참조.

## 이 파일을 고칠 때

- 서비스를 새로 붙이면 표에 한 줄 추가하고, 키 이름을 여기서 먼저 정한다 (코드가 이 이름을 쓴다).
- 상태가 바뀌면(대기 → 사용중) 날짜와 첫 실호출 결과를 한 줄 적는다.
