# 가족(볼케이노 제작자) 인터뷰 — 강제 규칙 · 검증된 프로세스 · 물어볼 것

기록일 2026-08-16. 전달받은 내용은 **원문 취지 그대로** 적는다. 해석·설계는 `장면이해_설계안.md` 에서 한다.

## 전달받은 내용 (원문 취지)

**검증된 프로세스는 두 단계다.**
- **① 분석 단계**: 레퍼런스 채널 칼카피 — 대본·자막·스타일을 제미나이 + 클로드로 분석해 **지침서를 고도화**한다.
- **② 구현 단계**: 영상 + 지침서 → **구글 AI Studio 제미나이 멀티모달** → 대본 + 편집점 타임코드 → TTS 나레이션 → 프리미어 자막·컷편집.

**강제 규칙 (가장 중요):**
> 영상 분석은 제미나이 멀티모달이 영상 **"전체"** 를 직접 봐야 한다.
> 클로드 코드는 멀티모달이 안 되어 프레임 추출 방식인데, 이는 **열등한 폴백**이다.
> **제미나이를 붙이는 것이 이 파이프라인의 핵심이다.**

## 우리 현재와의 거리 (사실만)

| 가족 기준 | 우리 지금 (v0.5-export) | 판정 |
| :---- | :---- | :---- |
| 제미나이가 영화 **전체**를 본다 | brief = 전사 **텍스트만** · select = 무음 구간 **프레임 5s 간격 + 결말 클립 15s** 만 인라인 전달 | **미달** — 폴백 방식 |
| 지침서를 영상과 함께 준다 | brief/select 프롬프트에 규격 일부만 | 미달 |
| 영상 전달 = AI Studio 웹에 원본 첨부(사람) — Q2 답 | 서버→runner 자동 호출(EvoLink 인라인) | 다름 — 장면이해는 need_input(사람 절차)로 설계 |
| 제미나이 출력 = 대본 + 편집점 타임코드 | select 는 서버가 우선순위 채움 · script 는 클로드가 대화 안에서 | 다름 (의도적 — 조건만 적고 방법은 클로드) |
| ① 분석 단계(칼카피 → 지침서 고도화) | 정답지.json(지무비 19편 대역) + 나레이션.md/참고_대본지침.md — 한 번 만든 뒤 고도화 루프 없음 | 미달 |

## 인터뷰 질문 (우선순위 순)

- **Q1.** 완성 prproj 는 어떻게 만들어? 미리 만든 틀(donor) prproj 를 복제해서 내용을 바꿔치기하는 방식이야?
  - **증거상 답 (2026-08-16, 전달본 분석 — `진단일지.md` 9절)**: 그렇다. 참고 폴더 `longformmovie_이관/assets/donor/real-edited-2026-v45-25fps.prproj` 를 뼈대로 `pipeline/stages/assemble.py` 가 서브트리 복제·ObjectID 재배선·Source Text 블롭(FlatBuffers) 치환 → gzip 재직렬화. 전달본에 도너 잔재(빈 V5·빈 캡션 트랙·고아 클립 9·Mac 경로) 남아 있음. **말로 확인할 것**:
    - Q1-a. 도너 `real-edited-2026-v45-25fps.prproj` 는 누가·어느 프리미어(Mac 2025 영어 UI?)에서 편집한 것이고, 프리미어 버전이 바뀌면 도너를 다시 만들어야 하나?
    - Q1-b. Source Text 블롭은 캡처한 실물 1개(GT 724B)를 패치하는 방식 — 폰트/크기/색 런 외에 못 바꾸는 것(정렬·행간·외곽선)이 있나?
    - Q1-c. 빌드 뒤 프리미어에서 열어 저장하는 단계가 있나, 아니면 "열면 완성" 그대로 전달하나? 열었을 때 깨진 적(미디어 오프라인·폰트)은?
    - Q1-d. 캡션 트랙(비어 있음)·TranscriptClip·고아 컷 9개(Horizontal Flip)는 의도적 잔재인가 도너에서 딸려온 것인가?
    - Q1-e. 게이트 96개 목록(어떤 항목·대역)을 파일로 받을 수 있나? (`참고_게이트.md` 99종과 대조)
    - Q1-f. 컷 시작/끝을 ms 단위로 두는 이유(프레임 스냅 안 함) — 프리미어에서 문제 없나?
  - **답 (2026-08-17, 가족 말)**: "donor prproj 활용한 적 없다. 기존에 쓰던 프리미어 파일을 던져주고 구현했다."
  - **해석(기록)**: 우리 분석과 **사실상 같은 실체**다. 참고 폴더 `assets/donor/real-edited-2026-v45-25fps.prproj` 가 바로 그 "기존에 쓰던 프리미어 파일"이고, 가족은 방식의 이름(도너 복제)을 몰랐을 뿐 실체는 **기존 파일 복제·치환**이 맞다(assemble.py 의 `DONOR` 표·"도너 서브트리 복제 → 재배선 → 블롭 치환"). Q1-a~f 는 그대로 유효(특히 a: 그 파일이 어느 프리미어에서 온 것인가). → 이행 계획: `prproj생성_설계안.md`(우리 도너는 사용자 프리미어로 새로 만든다).
- **Q2.** 제미나이에 영상 전체를 어떻게 넣어? (Files API 자동? AI Studio 에 수동 업로드? 지침서는 어느 시점에 같이 줘?)
  - **답 (2026-08-16)**: **Files API 는 쓰지 않는다.** 구글 AI Studio 에 **원본 영상을 그대로 첨부**하는 형태다 = 사람이 웹에서 영상 전체 + 지침서를 직접 첨부하고, 제미나이 출력을 받아온다. → 설계 반영: `장면이해_설계안.md` (runner 형태 = need_input, 사람 절차서). Files API 는 2차 자동화 후보로 강등.
- **Q3.** 제미나이가 뱉는 출력 형식은? (대본 + 편집점 타임코드가 한 번에? 몇 번에 나눠서?)
  - **미결** — 목표는 말로 듣는 것이 아니라 **가족의 실제 출력물 샘플(제미나이가 뱉은 원문 파일) 1개를 확보**하는 것. 그 형식이 scene.json 스키마와 절차서의 "출력 저장" 칸을 정한다.
- **Q4.** 프리미어프로용 XML 3종은 어떻게 만들었고 각각 용도가 뭐야?
- **Q5.** 분석 단계(레퍼런스 칼카피)는 얼마나 자주 돌려? 지침서 고도화는 어떤 형식으로 쌓여?

답을 받으면 여기 각 질문 밑에 **답 · 날짜**를 적고, 설계에 반영한 것은 해당 문서를 가리킨다.

## 관련
- 오독 정정(왜 지금까지 "인라인이면 충분"이라 적었는지): `진단일지.md` 7절
- 새 단계 설계안(구현 전 보고용): `장면이해_설계안.md`
- 외부 API 목록: `외부서비스.md`
