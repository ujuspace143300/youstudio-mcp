# HARNESS.md — 설계 원칙

프로젝트별 운용 규칙은 `CLAUDE.md` 참조.

이 프로젝트는 **영상 제작 하네스**다. 사람이 방향을 정하고, 클로드가 실행한다. 이 파일은 목차다. 상세는 각 문서에 있다. **이 파일의 내용을 100줄 넘게 키우지 마라(빈 줄·표 여백 제외).** 길어지면 상세를 별도 문서로 빼고 여기엔 가리키는 줄만 남긴다.

출처: OpenAI Harness Engineering (openai.com/ko-KR/index/harness-engineering/) 적용일: 2026-08-14

---

## 0\. 한 줄

> **사람은 방향을 정하고, 에이전트가 실행한다.** 내 일은 코드를 쓰는 게 아니라 **환경·기준·피드백 루프를 설계하는 것**이다.

- **강제 규칙(가족, 2026-08-16)**: 영상 분석은 제미나이 멀티모달이 **전체**를 본다. 프레임 추출은 폴백일 뿐이다. 원문·인터뷰 → `설계/가족인터뷰.md`, 설계안 → `설계/장면이해_설계안.md`
- **강제 규칙(사용자, 2026-08-17)**: 대사 자막 타이밍은 단어 단위 **실측** 타임스탬프로 잡는다. 문장 덩어리(세그먼트) 추정 금지. 현재 1순위 도구 = Speechmatics (상세: `설계/외부서비스.md`)
- **강제 규칙(사용자, 2026-08-18)**: **결말은 공개한다.** 지무비가 0/10 비공개인 것은 알고 있으며 우리는 다른 길을 택했다 — 격차가 아니라 **우리 편집 방침**이다. 「지무비와 다르다」는 이유로 이 항목을 되돌리자는 제안을 하지 마라. (근거·기록: `분석/지무비/종합_1차.md`)
- **강제 규칙(사용자, 2026-08-20)**: **기존 값을 덮기 전에 같은 축으로 다시 잰다. 축이 다르면 덮지 말고 병기한다.** 가족 실측(나레 95~108px)을 「다른 자로 잰 값」이라며 잉크 %로 덮었다가 자막이 30~40% 작아졌다 — 재계측 결과 옛 값이 옳았다. (사고 기록: `설계/진단일지.md` §25)
- **강제 규칙(사용자, 2026-08-17)**: 배포(클라우드·지인) 전에는 **모든 API 키를 재발급(초기화)** 한다. 예외 없음. (체크리스트: `설계/외부서비스.md`)

---

## 1\. 보이지 않으면 존재하지 않는다

가장 중요한 원칙. 원문 표현으로 **"에이전트가 볼 수 없는 것은 존재하지 않는다."**

- 머릿속의 편집 감각, 카톡으로 오간 판단, 지난 영상에서 배운 것 — **파일에 적히지 않으면 없는 것이다.**  
- 영상을 만들다 "이런 건 빼는 게 낫다"를 발견하면 **그날 지침서에 적는다.** 안 적으면 다음에도 똑같이 틀린다.  
- 판단의 근거가 되는 수치는 어디서 나왔는지 함께 적는다.

- **고치기 전에 최소 테스트로 원인을 격리한다.** 증상이 A 에서 보여도 범인이 A 라는 증거는 아니다 — 오디오 트랙이 안 생겼을 때 XML 은 무죄였고 프리미어 상태가 원인이었다. 의심 가는 것 **하나만** 다른 최소 파일로 확인한 뒤 고친다.

**검사법** — 새 대화창을 열고 클로드에게 시켰을 때 같은 결과가 나오는가? 안 나오면 어딘가 적히지 않은 지식이 있다.

---

## 2\. 문서는 목차 \+ 분리된 조각

거대한 단일 문서는 금지. 원문 경고 — 모든 게 중요해 보이면 지시가 힘을 잃고, 파일은 무덤이 된다.

스타일 지침서의 조각 구성(`STYLE.md` 목차 · `정답지.json` 목표 · `규격.json` 설정 · `우리실측.json` 결과 · 구간선택·나레이션·자막)은 `CLAUDE.md` 「폴더 구조」에 그려져 있다. **같은 트리를 두 곳에 두지 않는다.**

**단계마다 필요한 문서만 내려보낸다.** 대본 쓸 때 자막 규칙까지 읽히지 않는다.

---

## 3\. 방법이 아니라 조건을 적는다

원문 안티패턴 — **과도한 명세.** 어떻게 풀지 지시하지 말고 무엇이 참이어야 하는지만 제약하라.

|  |  |
| :---- | :---- |
| ❌ | "3초마다 컷을 끊어라" |
| ✅ | "컷 평균 길이가 **2.0\~3.5초 대역** 안에 들어와야 한다" |

방법은 클로드가 찾고, 조건 충족만 검사한다.

**참고** — 롱폼 파이프라인의 `결정대기_8건.md`가 이미 전부 대역 형태로 쓰여 있다. 그 형식을 그대로 이어받는다.

---

## 4\. 결과를 숫자로 뱉는다 (관측 가능성)

클로드가 자기 결과를 볼 수 없으면 눈 감고 일하는 것이다. **모든 단계는 지표를 함께 반환한다.**

| 단계 | 함께 뱉을 숫자 |
| :---- | :---- |
| 구간 선택 | 구간 수 · 총 길이 · 평균 길이 · 원본 대비 비율 |
| 나레이션 | 나레:대사 비율 · 문장 수 · 평균 자수 |
| 자막 | 큐 수 · 분당 큐 밀도 · 한 줄 최대 자수 |
| 조립 | 컷 수 · 평균 컷 길이 · 장면 전환율 |

---

## 5\. 게이트는 고치는 법까지 말한다

원문 실무 항목 — 오류 메시지가 **수리 지침을 에이전트 컨텍스트에 주입**한다.

|  |  |
| :---- | :---- |
| ❌ | "컷 길이 위반" |
| ✅ | "컷 평균 4.8초. 대역 2.0\~3.5초. **긴 구간 상위 5개를 반으로 쪼개거나, 설명이 긴 구간을 나레이션으로 대체하라.**" |

**게이트는 시각·수치만이 아니라 내용도 본다.** 새 산출물에는 「그 자리에 있어야 할 **바로 그 내용**인가」를 되받아 확인하는 게이트를 하나 붙인다 — 소리는 ASR 로 받아 적어 대조하고, 번역은 원문과 대조한다. (전례: `G-나레문구일치`·`G-대사원문일치`·`G-자막음성일치`)

게이트는 단계마다 붙는다. **전체 실행을 검증 수단으로 쓰지 않는다** — 실패할 때마다 시간과 돈이 나간다.

---

## 6\. 책 원문 요약 (4요소 · 내 역할 · 기대치)

하네스 4요소 대응표 · 인간의 일 · 초기 속도 기대치 → `설계/하네스_원문요약.md`. **매 단계에서 실제로 쓰는 원칙은 위 1~5 절이 전부다.**

---

## 7\. 이 파일을 고칠 때

- 원칙이 늘어나면 **여기 요약만 남기고 상세는 별도 파일로** 뺀다.  
- 실제와 문서가 어긋나면 **문서를 고친다.** 어긋난 채로 두면 다음 사람(=미래의 나)이 속는다.  
- 규칙 자체가 부정확해서 어긋난 경우에는 결과물이 아니라 규칙을 고친다.

